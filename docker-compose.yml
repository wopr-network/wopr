services:
  wopr:
    build: .
    container_name: wopr
    restart: unless-stopped
    volumes:
      - wopr-data:/data
    environment:
      - WOPR_HOME=/data
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    ports:
      - "49737:49737/udp"

volumes:
  wopr-data:
